apiVersion: v1
kind: Pod
metadata:
  name: aspnetapp-pod
  labels:
    name: aspnetapp
spec:
  imagePullSecrets:
    - name: acr-secret
  containers:
    - name: aspnetapp-container
      image: acr101.azurecr.io/sept101image:73
      imagePullPolicy: Always
      resources: 
        requests:
          memory: "256Mi"
          cpu: "250m"
        limits:
          memory: "512Mi"
          cpu: "500m"
      ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: aspnet-service
spec:
  selector:
    name: aspnetapp
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer   
